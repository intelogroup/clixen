# Clixen local auth — the Next.js demo app proxies to the real local backend
# (tools-harness/chat_ui.py). Its auth endpoints (register/login/logout/reset)
# are the single source of truth; no cloud auth is involved.
CLIXEN_API_URL=http://127.0.0.1:9234
